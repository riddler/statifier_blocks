# The fixture project's instructions

Instructions cite records by line just as records cite each other, which is
why this file is a source.

The instructions quote the first decision, "the arm gets a field"
(`docs/adr/0002-cited.md:3`), and that line has not moved.
